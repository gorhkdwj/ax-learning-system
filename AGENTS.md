# AX 학습공간 에이전트 지침

## 언어와 대화

- 모든 사용자 응답은 격식 있는 한국어 합쇼체로 작성합니다.
- 확인된 사실, 해석, 가정, 불확실성을 구분합니다.
- 학습 범위나 난이도를 크게 바꾸는 판단은 사용자와 함께 결정합니다.

## 이 작업공간의 목적

이 작업공간은 특정 직무 하나에 한정되지 않는 전사적 AI Transformation 역량을
학습하기 위한 공간입니다. 현재 업무는 학습 우선순위를 정하는 근거로 사용하지만,
전체 역량지도와 학습 라이브러리의 범위를 현재 업무에 종속시키지 않습니다.

이 디렉터리는 공개 가능한 학습 시스템 저장소입니다. 선택적 비공개 원천은 형제
`../ax-learning-vault/`에 두며, 상세 경계는
`docs/architecture/public-private-storage.md`를 따릅니다. 이 저장소는 Vault가
없어도 검증과 공개 예제로 정상 작동해야 합니다.

작업 전 다음 문서를 순서대로 확인합니다.

1. `docs/governance/learning-governance.md`
2. `docs/architecture/learning-system.md`
3. `docs/research/trend-signal-governance.md`
4. `docs/plans/curriculum-foundation-plan.md`

원문 수집, 저장소 경계, 임베딩 또는 HUB 데이터 로딩 작업에서는 추가로
`docs/architecture/public-private-storage.md`를 확인합니다.

Phase 2 전수조사 작업에서는 추가로
`docs/research/phase2-capability-survey-runbook.md`와 현재 Wave Checkpoint를
확인합니다.

## 핵심 구조

학습 시스템은 다음 네 층을 분리하여 관리합니다.

1. 전사 AX 역량지도
2. 재사용 가능한 기술·개념 Learning Unit
3. 업무·파이프라인·산출물별 Learning Set
4. 사용자별 동적 우선순위 학습 경로

Learning Set은 Unit의 내용을 복제하지 않고 정규 ID와 버전으로 참조합니다.
공통 지식은 Unit에서, 업무 맥락과 연결 실습 및 통합 평가는 Set에서 관리합니다.

신흥 용어·개념·패턴은 네 학습 계층에 바로 넣지 않고 Trend Signal 연구 인입
레코드로 먼저 조사합니다. Trend Signal은 다섯 번째 학습 계층이나 이수 대상이
아니며, 검증 후 기존 항목 병합·Resource·Adapter·Unit·Probe Set·Set·관찰·기각
중 적합한 목적지로 연결합니다.

## 필수 작업 원칙

- 원문 전문, 개인 기록, 임베딩과 비공개 생성물을 이 공개 저장소에 넣지 않습니다.
- Vault 경로를 직접 하드코딩하지 않고 `AX_VAULT_ROOT`와 공통 경로 해석기를
  사용합니다.
- 기술 분류를 무한히 확장하지 않습니다. 선정·제외·보류 사유를 기록합니다.
- 전체 카탈로그 등록과 상세 교재 제작 및 실제 학습 배정을 구분합니다.
- 모든 Unit, Set, Resource, Trend Signal과 조사 Candidate는 해당 JSON Schema에
  맞는 메타데이터를 가집니다.
- 새로 유행하는 명칭만으로 Unit을 만들지 않습니다. 정의 신뢰도와 효과 신뢰도를
  분리하고 claim과 evidence를 연결한 뒤 승격 게이트를 통과시킵니다.
- 파일명이나 본문을 추측하여 학습 구조를 구성하지 않습니다.
- 필수·선택 구분, 실행 우선순위, 선행관계를 서로 다른 축으로 관리합니다.
- 보안·개인정보·권한·비용·접근성·관측성·복구를 횡단 품질축으로 적용합니다.
- AI 리터러시, 결과 검증, 저작권·라이선스와 책임 있는 사용을 공통 역량으로 다룹니다.
- AI 에이전트보다 결정적 자동화나 기존 SaaS가 적합한지 먼저 판단합니다.
- 고위험·비가역 외부 변경에는 인간 승인과 실행 후 상태 검증을 둡니다.
- 코드 전체 독해를 기본 검증법으로 삼지 않습니다. 자동검증, 로그, 추적,
  실제 결과 상태, 표본 및 위험 경계 검토를 조합합니다.
- 공식 문서·표준·원 논문·공식 소스코드를 우선하며, 출처·버전·확인일을 기록합니다.
- 틀린 내용을 학습시키지 않는 것을 가장 높은 품질 기준으로 둡니다.

## 학습자료 원칙

- HTML은 학습 허브와 상호작용 자료의 우선 고려 형식이지만 강제하지 않습니다.
- 실제 기술을 배울 때는 해당 기술의 원형 도구와 실행 환경을 사용합니다.
- 코드 주석은 문법 반복보다 의도, 입력·출력 계약, 제약, 실패 조건,
  설계 이유와 검증 방법을 설명합니다.
- 경로는 작업공간 기준 상대경로를 사용하며 사용자 컴퓨터의 절대경로를 넣지 않습니다.
- 의존성 버전, 잠금 파일, 환경변수 예시, 실행·검증 명령을 제공합니다.
- 생성형 HUB 산출물은 직접 수정하지 않고 메타데이터와 생성기를 수정합니다.
- 상호작용 자료에는 키보드 접근, 텍스트 대체, 필요한 경우 정적·인쇄 대체본을 제공합니다.

## 숙련도

- D0 인지: 용도와 위험을 설명합니다.
- D1 안내 수행: 예제를 따라 결과물을 만듭니다.
- D2 독립 수행: 새로운 소규모 업무를 구현하고 자동검증합니다.
- D3 운영 수행: 권한·보안·비용·관측성·장애·롤백까지 관리합니다.
- D4 전문 최적화: 내부 구현이나 프레임워크 자체를 연구·개발합니다.

보조 도구는 원칙적으로 D2, 핵심 AX 파이프라인·에이전트·배포·보안은 D3를
기본 목표로 합니다. D4는 실제 필요가 입증된 경우에만 포함합니다.

유지보수·운영·개선·기능 추가가 현재 Unit의 범위나 난이도를 크게 넘으면
운영 심화 트랙, 별도 Unit, Set 통합 실습, 보류·기각 중 하나로 분리하고
중대한 범위 변경은 사용자와 함께 판단합니다.

## 검증

- 스키마나 메타데이터를 변경하면 JSON 구문과 스키마 정합성을 확인합니다.
- Unit과 Set 참조의 누락, 중복 ID, 선행관계 순환, 지원 종료 버전을 검사합니다.
- Trend Signal의 상태 전이, 주장·근거 연결, 동의어 충돌, 정확한 버전 참조와
  승격 대상을 검사합니다.
- 조사 Candidate의 스키마, 중복 ID·명칭, 정확한 병합 대상, 출처 날짜와
  판정·목적지 정합성을 검사합니다.
- MVP의 Unit, Set, Resource 참조는 정규 ID와 정확한 콘텐츠 버전으로 고정합니다.
- 자동 검사 통과와 업무적으로 올바른 결과를 구분합니다.
- 검증하지 못한 자료는 확인된 것으로 표시하지 않습니다.
