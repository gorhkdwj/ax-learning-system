# Optional private Vault location, resolved from this public repository root.
# The public system must continue to work when this directory does not exist.
# This example is not auto-loaded; export it to the process environment or let
# a future runtime load an untracked .env.local explicitly.
AX_VAULT_ROOT=../ax-learning-vault
